Docker placeholders

Add Dockerfiles and compose files here for building the backend, frontend, and wasm core.

Example next steps:

- `docker/backend/Dockerfile`
- `docker/frontend/Dockerfile`
- `docker/compose.yml`
